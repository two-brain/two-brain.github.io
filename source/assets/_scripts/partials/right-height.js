/*
 * Right Height - Dynamically set content areas of
 * different lengths to the same height
 *
 * https://github.com/cferdinandi/right-height
 *
 * by Chris Ferdinandi
 */

import rightHeight from 'Right-Height';

export default function rightHeightJS() {
  rightHeight.init();
}
